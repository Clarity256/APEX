# LaTeX Templates

This directory stores upstream template packages and documentation. Active paper
drafts should not depend on files here during normal compilation, except when a
template update is intentionally copied into a paper working directory.
