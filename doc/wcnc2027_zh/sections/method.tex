% !TeX root = ../main.tex

\section{方法}

本文方法包含内外两层。内层在固定归属下求解 PRB--功率联合分配问题，用作真实效用
评估器和外层代理校准目标。外层在慢时隙尺度上求解归属路径，显式满足可见性和切换
预算约束。

小规模场景中，外层问题可用混合整数规划得到离线最优基线。较长时间范围或更大规模
场景中，可采用滚动窗口方法：每次只优化一个有限窗口，提交窗口前若干时隙的归属，
并将最后一次归属和剩余切换预算传递到下一个窗口。该方法牺牲部分全局最优性，但能
显著降低求解时间，同时保持硬切换预算的可行性。
